[project]
name = "ai-video-audio-analyser"
version = "0.1.0"
description = "Serverless AI pipelines for video and audio analysis on Modal"
requires-python = ">=3.12"
dependencies = [
    "modal",
    "python-dotenv",
    "requests",
    "numpy",
]
